language: cpp
sudo: false
addons:
  apt:
    packages:
      - bison
      - flex
      - automake
script:
  - aclocal
  - autoconf
  - ./configure --with-mysql --with-mysql-lib=$(mysql_config --variable=pkglibdir)
  - make
